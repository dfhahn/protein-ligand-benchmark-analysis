name: test
channels: 
  - omnia
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - os
  - re

  - numpy
  - scipy
  - plotly
  - pandas
  - rdkit
  - svgutils
  - pint
  - tqdm

    # Pip-only installs
  #- pip:
  #  - codecov

